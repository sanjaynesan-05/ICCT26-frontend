# MINIMAL NETLIFY CONFIGURATION - Use this if build errors persist
# Simply replace your current netlify.toml with this version

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
