# 🏗️ Data Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                      React Components                            │
│  (Schedule, Rules, Contact, Registration, Home, etc.)            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
        ┌────────────────────────────────┐
        │  Data Import Points (@/data)   │
        │  - MATCHES                     │
        │  - TOURNAMENT_RULES            │
        │  - ORGANIZERS                  │
        │  - REGISTRATION_STEPS          │
        │  - ANNOUNCEMENTS               │
        └────────────────┬───────────────┘
                         │
        ┌────────────────┴──────────────────────────────┐
        │                                               │
        ▼                                               ▼
    ┌─────────────┐                          ┌──────────────────┐
    │ Data Files  │                          │ Config Files     │
    ├─────────────┤                          ├──────────────────┤
    │ schedule.ts │◄──────────┐              │ app.config.ts    │
    │ rules.ts    │           │              └──────────────────┤
    │ contact.ts  │           │              - Colors           │
    │ registration.ts         │              - Typography       │
    │ home.ts     │           │              - Routes           │
    │ index.ts    ◄───────────┘              - Animations       │
    └─────────────┘                          - API Config       │
         │                                   - Feature Flags    │
         │                                   └──────────────────┘
         └──────────────┬──────────────────────────────────────┬──┐
                        │                                       │  │
                        ▼                                       │  │
    ┌──────────────────────────────────┐                      │  │
    │    Type Definitions              │                      │  │
    │    (@/types)                     │                      │  │
    ├──────────────────────────────────┤                      │  │
    │ - Match                          │                      │  │
    │ - Rule                           │                      │  │
    │ - Organizer                      │                      │  │
    │ - SocialLink                     │                      │  │
    │ - FormData                       │                      │  │
    │ - RegistrationStep               │                      │  │
    │ - Announcement                   │                      │  │
    │ - Venue                          │                      │  │
    │ - AppConfig                      │                      │  │
    │ - Player                         │                      │  │
    └──────────────────────────────────┘                      │  │
                        ▲                                      │  │
                        │                                      ▼  ▼
                        └──────────────────────────────────────┴──┘
```

## Data Flow Diagram

```
User Component
      │
      ▼
Import Data/Config
      │
      ├─► MATCHES
      │     │
      │     └─► getMatchesByDate()
      │     └─► getMatchById()
      │     └─► getUpcomingMatches()
      │
      ├─► TOURNAMENT_RULES
      │     │
      │     └─► getRuleById()
      │     └─► getRuleContentByTitle()
      │
      ├─► ORGANIZERS
      │     │
      │     └─► getOrganizerByRole()
      │
      ├─► REGISTRATION_STEPS
      │     │
      │     └─► getStepByNumber()
      │     └─► calculateProgress()
      │     └─► validateFile()
      │
      ├─► ANNOUNCEMENTS
      │     │
      │     └─► getAnnouncementById()
      │     └─► getRandomAnnouncement()
      │
      └─► APP_CONFIG
            │
            └─► isFeatureEnabled()
            └─► COLOR_PALETTE
            └─► ROUTES
      │
      ▼
Display in UI
```

## Component Dependency Map

```
┌──────────────────────────────────────────────────────────┐
│                      App.tsx                              │
│              (Router Setup)                               │
└────────────────┬──────────────────────────────────────┬──┘
                 │                                      │
    ┌────────────┴────────┐                    ┌────────┴──────────┐
    │                     │                    │                   │
    ▼                     ▼                    ▼                   ▼
┌─────────┐           ┌─────────┐        ┌────────────┐    ┌───────────┐
│ Navbar  │           │ Footer  │        │ Countdown  │    │ Ticker    │
│         │           │         │        │            │    │           │
│ Uses:   │           │ Uses:   │        │ Uses:      │    │ Uses:     │
│ ROUTES  │           │ ROUTES  │        │ APP_CONFIG │    │ ANNOUNCE- │
│         │           │         │        │            │    │ MENTS     │
└─────────┘           └─────────┘        └────────────┘    └───────────┘
    │                                          │
    │                                          │
    ├─ Home Page         ◄─────────┬──────────┴─────────────┐
    │  Uses: ANNOUNCEMENTS                                  │
    │         TOURNAMENT_HIGHLIGHTS                         │
    │                                                       │
    ├─ Schedule Page     ◄──────────────────────┐           │
    │  Uses: MATCHES                            │           │
    │         getMatchesByDate()                │           │
    │         UNIQUE_MATCH_DATES                │           │
    │                                           │           │
    ├─ Rules Page        ◄──────────────────────┤           │
    │  Uses: TOURNAMENT_RULES                   │           │
    │         getRuleById()                     │           │
    │                                           │           │
    ├─ Registration Page ◄──────────────────────┤           │
    │  Uses: REGISTRATION_STEPS                 │           │
    │         REGISTRATION_CONFIG               │           │
    │         UPI_CONFIG                        │           │
    │         validateFile()                    │           │
    │         calculateProgress()               │           │
    │                                           │           │
    └─ Contact Page      ◄──────────────────────┘           │
       Uses: ORGANIZERS                                    │
              SOCIAL_LINKS                                 │
              VENUE                                        │
              getOrganizerByRole()                         │
              getSocialLinkByName()                        │
              getFormattedVenueAddress()                   │
```

## Type System Flow

```
┌─────────────────────────────────────────────┐
│         types/index.ts                       │
│  (Central Type Definitions)                  │
├─────────────────────────────────────────────┤
│                                              │
│  ┌──────────────┐  ┌──────────────┐        │
│  │   Match      │  │   Rule       │        │
│  ├──────────────┤  ├──────────────┤        │
│  │ id: number   │  │ id: number   │        │
│  │ match: str   │  │ title: str   │        │
│  │ date: str    │  │ icon: str    │        │
│  │ time: str    │  │ content: []  │        │
│  │ venue: str   │  │              │        │
│  │ teamA: str   │  │              │        │
│  │ teamB: str   │  │              │        │
│  │ status: enum │  │              │        │
│  └──────────────┘  └──────────────┘        │
│                                              │
│  ┌──────────────┐  ┌──────────────┐        │
│  │ Organizer    │  │ SocialLink   │        │
│  ├──────────────┤  ├──────────────┤        │
│  │ role: str    │  │ name: str    │        │
│  │ name: str    │  │ icon: React  │        │
│  │ phone: str   │  │ url: str     │        │
│  │ whatsapp: str│  │ color: str   │        │
│  │ email: str   │  │              │        │
│  └──────────────┘  └──────────────┘        │
│                                              │
│  ┌──────────────────┐                       │
│  │ RegistrationStep │                       │
│  ├──────────────────┤                       │
│  │ number: number   │                       │
│  │ title: str       │                       │
│  └──────────────────┘                       │
│                                              │
└─────────────────────────────────────────────┘
        ▲         ▲         ▲         ▲
        │         │         │         │
        │         │         │         │
   schedule   rules    contact   registration
       .ts      .ts       .ts         .ts
```

## Configuration Hierarchy

```
App Configuration (app.config.ts)
    │
    ├─ APP_CONFIG
    │   ├─ tournamentName
    │   ├─ tournamentYear
    │   ├─ countdownTargetDate
    │   ├─ registrationFee
    │   ├─ upiId
    │   └─ socialMedia
    │
    ├─ COLOR_PALETTE
    │   ├─ primary (#002B5C)
    │   ├─ secondary (#0D1B2A)
    │   ├─ accent (#FFCC29)
    │   ├─ bgStart
    │   ├─ textPrimary
    │   └─ textSecondary
    │
    ├─ TYPOGRAPHY
    │   ├─ fontFamily
    │   └─ fontSize
    │
    ├─ ROUTES
    │   ├─ home
    │   ├─ schedule
    │   ├─ registration
    │   ├─ rules
    │   └─ contact
    │
    ├─ ANIMATIONS
    │   ├─ duration
    │   └─ delay
    │
    ├─ API_CONFIG
    │   ├─ baseUrl
    │   ├─ timeout
    │   └─ retries
    │
    └─ FEATURE_FLAGS
        ├─ enableRegistration
        ├─ enableLiveScore
        ├─ enableGallery
        └─ enableTeamLeaderboard
```

## Data Organization Layers

```
┌──────────────────────────────────────────────────────────────┐
│                    Layer 1: Components                        │
│            (React Components using data)                      │
└──────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────┐
│                    Layer 2: Data Import                       │
│         (Central imports from @/data or @/config)            │
└──────────────────────────────────────────────────────────────┘
                              │
                    ┌─────────┼─────────┐
                    │         │         │
                    ▼         ▼         ▼
┌──────────────────────┐  ┌──────────┐  ┌──────────────┐
│  Layer 3: Data       │  │ Layer 3: │  │ Layer 3:     │
│  Constants & Utils   │  │ Exported │  │ Type-Safe    │
│                      │  │ Functions│  │ Interfaces   │
│ - schedule.ts        │  │ - Helper │  │ - types/     │
│ - rules.ts           │  │   funcs  │  │   index.ts   │
│ - contact.ts         │  │          │  │              │
│ - registration.ts    │  │          │  │              │
│ - home.ts            │  │          │  │              │
└──────────────────────┘  └──────────┘  └──────────────┘
        │                    │                  │
        └────────────────────┼──────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────┐
│              Layer 4: Static Data                             │
│       (Immutable, compile-time optimized)                    │
└──────────────────────────────────────────────────────────────┘
```

## Import Hierarchy

```
┌─────────────────────────────────────────────────────────────┐
│                    src/data/index.ts                         │
│         (Central Export - Import everything here)            │
├─────────────────────────────────────────────────────────────┤
│  export { MATCHES, ... } from './schedule'                   │
│  export { TOURNAMENT_RULES, ... } from './rules'             │
│  export { ORGANIZERS, ... } from './contact'                 │
│  export { REGISTRATION_STEPS, ... } from './registration'    │
│  export { ANNOUNCEMENTS, ... } from './home'                 │
└─────────────────────────────────────────────────────────────┘
        │         │          │           │          │
        ▼         ▼          ▼           ▼          ▼
    schedule   rules      contact   registration   home
       ts        ts         ts           ts         ts
```

---

This architecture ensures:
- ✅ **Separation of Concerns** - Each layer has a specific role
- ✅ **Type Safety** - Full TypeScript support throughout
- ✅ **Maintainability** - Clear structure and organization
- ✅ **Scalability** - Easy to add new data layers
- ✅ **Performance** - Static data optimized at build time
- ✅ **Reusability** - Components can import data independently
